# Optional: full origin of the parking API (no trailing slash).
# Leave unset during local dev so the app calls same-origin `/api/...` and Vite proxies to the backend.
# VITE_API_BASE_URL=http://127.0.0.1:3001
